export enum APIEndpoints {
  Register = '/users',
  Login = '/users-login',
  Logout = '/users-logout',
  DeleteUser = '/users-delete',
  UpdateUser = '/users-update',
  Goals = '/goals',
  Categories = '/categories',
}

export enum Routes {
  Home = '/',
  Goals = '/goals',
  SingleGoal = '/goal',
  UserPage = '/user',
  Login = '/login',
}
